{
  "name": "posts",
  "version": "0.1.0",
  "scripts": {
    "lint": "textlint ./**/*.md || markdownlint-cli2 ./**/*.md",
    "lint:fix": "textlint --fix ./**/*.md || markdownlint-cli2 --fix ./**/*.md",
    "dev": "zenn preview"
  },
  "devDependencies": {
    "zenn-cli": "^0.1.151"
  }
}
